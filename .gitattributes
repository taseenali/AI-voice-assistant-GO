# Normalize all text files to LF in the repo; let git convert on checkout per platform
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.db binary
*.db-wal binary
*.db-shm binary
