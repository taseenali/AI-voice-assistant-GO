The MVAIR waveform mark and full logo lockup — recreated from the product source.

```jsx
import { MvairMark, MvairLogo } from './MvairMark';

<MvairMark tone="petrol" size={30} />          {/* glyph on light */}
<MvairLogo tone="aqua" size={28} />            {/* mark + wordmark on dark */}
```

`tone="petrol"` on light surfaces, `tone="aqua"` on the dark canvas. Wordmark is Newsreader semibold.
