Session-channel badge with an icon — phone (teal) or web (slate). Put it first in session tables for fast visual scanning.

```jsx
import { ChannelBadge } from './ChannelBadge';

<ChannelBadge channel="phone" />
<ChannelBadge channel="web" />
```
