# API Contracts

Payload fixtures copied from official docs and used in vitest. **Do not invent shapes — update fixtures when Vapi docs change.**

- `vapi/` — Vapi server URL events ([docs](https://docs.vapi.ai/server-url/events))
- Test: `npm test -- tests/vapi-webhook.test.js`
